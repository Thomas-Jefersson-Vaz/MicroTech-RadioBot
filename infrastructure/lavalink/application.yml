server: # REST and WS server
  port: 2333
  address: 0.0.0.0
  http2:
    enabled: false # Whether to enable HTTP/2 support
plugins:
  youtube:
    enabled: true # Whether this source can be used.
    allowSearch: true # Whether "ytsearch:" and "ytmsearch:" can be used.
    allowDirectVideoIds: true # Whether just video IDs can match. If false, only complete URLs will be loaded.
    allowDirectPlaylistIds: true # Whether just playlist IDs can match. If false, only complete URLs will be loaded.
    clients:
      - TVHTML5EMBEDDED
      - WEB
      - ANDROID_MUSIC
      - MUSIC
      - MWEB
      - IOS
      - ANDROID_VR
    oauth:
      enabled: true
      # refreshToken is provided via PLUGINS_YOUTUBE_OAUTH_REFRESHTOKEN env var
      skipInitialization: false  # Set to true once you have a valid refreshToken
lavalink:
  plugins:
    - dependency: "dev.lavalink.youtube:youtube-plugin:1.18.2"
      repository: "https://maven.lavalink.dev/releases"
  server:
    password: "youshallnotpass"
    sources:
      youtube: false # Disabling built-in youtube to use the plugin
      bandcamp: true
      soundcloud: true
      twitch: true
      vimeo: true
      http: true
      local: false
    filters: # All filters are enabled by default
      volume: true
      equalizer: true
      karaoke: true
      timescale: true
      tremolo: true
      vibrato: true
      distortion: true
      rotation: true
      channelMix: true
      lowPass: true
    bufferDurationMs: 4000 # 4s buffer for stability at higher quality
    frameBufferDurationMs: 8000 # Increased output buffer headroom
    opusEncodingQuality: 8 # High quality — stability issues fixed via stuck event handling
    resamplingQuality: MEDIUM # Better resampling without excessive CPU
    trackStuckThresholdMs: 10000 # 10s before Lavalink fires TrackStuckEvent
    useSeekGhosting: true
    youtubePlaylistLoadLimit: 15 # Number of pages to load (~100 tracks/page)
    playerUpdateInterval: 2 # Faster updates for dashboard progress tracking
    youtubeSearchEnabled: true
    soundcloudSearchEnabled: true
    gc-warnings: false

metrics:
  prometheus:
    enabled: true
    endpoint: /metrics

sentry:
  dsn: ""
  environment: ""

logging:
  file:
    path: ./logs/
  level:
    root: INFO
    lavalink: INFO
  request:
    enabled: true
    includeClientInfo: true
    includeHeaders: false
    includeQueryString: true
    includePayload: true
    maxPayloadLength: 10000
